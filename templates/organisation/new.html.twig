{% extends 'base.html.twig' %}

{% block title %}Create organisation{% endblock %}

{% block body %}

<div class="example-wrapper">
    <a href="{{ path('organisation_index') }}">[list]</a>
    <h1>Create organisation</h1>
    {{ form_start(form) }}
        {{ form_rest(form) }}
        <input type="submit" value="Add" />
    {{ form_end(form) }}
</div>
{% endblock %}
