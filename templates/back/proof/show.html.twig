{% extends 'base.html.twig' %}

{% block title %}Proof{% endblock %}

{% block body %}
    <h1>Proof</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ proof.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ proof.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ proof.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('back_app_proof_index') }}">back to list</a>

    <a href="{{ path('back_app_proof_edit', {'id': proof.id}) }}">edit</a>

    {{ include('back/proof/_delete_form.html.twig') }}
{% endblock %}
